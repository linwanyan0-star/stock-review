# 板块资金流向跟踪 — 需求规格 v1.1（已实现）

> 状态：v1.1，2026-08-03 已完成 v1 全链路实现（8/3 完整 + 7/27-7/31 历史回填 + 归档页 + 首页/复盘页入口）。
> 定位：嵌入每日盘面复盘页的「板块资金流向追踪」模块 + 独立跨日查看器，每日自动记录、可回看。

---

## 1. 问题陈述（Why）
中线投资者的复盘核心是**在板块轮动中领先于人建仓、在亏钱效应扩散前撤离**。原框架散落在脑中、每天手动拼，易漏维度、难回溯。本模块把"亏钱/赚钱效应 → 成交排名 → 量价确认 → 逻辑延展性 → 拥挤度"固化成**每日自动记录**的观测，降低复盘成本、可历史回溯。

---

## 2. 已确认的决策（v1.0 基线，2026-08-03 用户拍板）
1. **入口形态**：嵌入「每日复盘页」(`days/YYYY-MM-DD.html`) 为主 + **独立跨日查看器** `sector-flow/index.html`（首页模块入口）为单独入口。
2. **跟踪板块来源**：每日自动观测；**板块粒度 = 申万二级**（data_sector ranking 原生粒度，字段最全）。
3. **top-5 板块卡片口径（已拍板）**：**B 为主口径**（成交前 50 个股按板块聚类，取代表度最高的前 5 大板块）；**A 为参考值**（该板块单码成交额排名，并排显示在卡片上）。
4. **户数/领先度（拥挤度）**：默认方案确认——**主力净流入方向 + 融资融券余额日变动方向合成三档（拥挤升温 / 中性 / 拥挤退潮）**；季度实际户数并列作保底锚，明确标注"估算非官方"。
5. **板块逻辑摘要**：首批留空，后续人工补。
6. **更新频率**：交易日 15:35 独立 agent+MCP 自动化，随 16:00 推送自动生成。
7. **种子板块**：无需；每日市场观测，须记录每日数据供后续查看。
8. **历史回填（已拍板）**：回填**今天 + 上周**交易日（2026-08-03 完整；7/27–7/31 尽力回填，仅支持最新快照的接口降级标"待更新"，诚实标注）。
9. **跨日查看器（已拍板）**：日期列表（倒序）→ 点击展开当日完整模块；v1 不做趋势折线图（数据均在 json 中，后续可加）。

---

## 3. 分析模型 → 页面结构（每日快照）
| 步骤 | 页面区块 | 数据 |
|---|---|---|
| ①② 双榜 | 「吸金榜 / 失血榜」：31 行业按资金净流入排名头尾 | 行业资金净流入 + 阶段涨跌 |
| ③ 成交排名 | 「成交前 50 个股」：全市场 Top50，标注板块 | 个股成交/净流入/板块标签 |
| ③ 板块强度 | 「板块成交集中度」：前 50 各大板块占比 | 集中度 |
| ④ 板块确认 | top-5 板块卡片：量比 + 价趋势（日 K 缩略）双升判定 | 单码 sector kline |
| ⑤ 逻辑延展性 | 板块逻辑摘要区（**首批留空**） | 人工/半自动 |
| ⑤ 拥挤度 | **主力净流入 + 融资融券余额变化** 合成方向 + 季度户数保底 | 见 §8 |

---

## 4. 目标（Goals）
- G1：打开当日复盘页 30 秒内定位吸金/失血方向。
- G2：top-5 板块卡片呈现量价趋势 + 成交集中度，确认趋势。
- G3：拥挤度信号（主力 + 融资融券 + 季度户数）辅助"是否该撤"判断。
- G4：每日数据落盘（`.json` + 渲染），跨日查看器可回看任意历史日。

---

## 5. 非目标（Non-goals）
- 不做买卖点/择时建议；不替代 watchlist（板块级 vs 个股级）；不盘中实时；不预测；不自动选股/交易。
- 板块逻辑摘要 v1 留空，不做新闻自动摘要（news 渠道不可用）。

---

## 6. 用户故事
- 作为中线投资者，我希望每日复盘页直接看到吸金/失血双榜，快速定位轮动。
- 作为中线投资者，我希望 top-5 成交板块卡片含量价与集中度，确认趋势。
- 作为中线投资者，我希望看到主力+融资融券合成的拥挤度，判断领先度。
- 作为中线投资者，我希望跨日查看器回看任意日期的板块流向，做复盘比对。

---

## 7. 需求（MoSCoW）
### P0
- **R1 双榜**：当日 31 行业按资金净流入排名，展示吸金头牌 + 失血尾部（含阶段涨跌）。
- **R2 成交前 50**：全市场成交 Top50 个股，标注 SW1 板块 + 当日净流入。
- **R3 top-5 板块卡片**：每日自动取成交前 5 板块，每卡含阶段涨幅、量比、价趋势、成交集中度。
- **R4 每日落盘**：数据写入 `days/YYYY-MM-DD.json` 的 `sectorFlow` 键，并在 `.html` 渲染；manifest 机制纳入归档。
- **R5 跨日查看器**：`sector-flow/index.html` 列出所有日期快照，点入看当日完整模块（单独入口）。

### P1
- **R6 板块逻辑摘要区**：每卡文本区（v1 留空，结构预留）。
- **R7 拥挤度信号条**：**主力净流入(方向+幅度) + 融资融券余额变化(方向+幅度)** 合成拥挤度方向；**季度实际户数**并列作保底锚，明确标注"估算非官方"。

### P2
- 板块内最强/最弱/平均个股对比自动化。
- 北向作拥挤度交叉验证。
- 板块逻辑新闻自动摘要。

---

## 8. 数据源与缺口（⚠️ 实现期须验证，B 已明确合成口径）
| 维度 | 计划数据源 | 缺口/风险 |
|---|---|---|
| 行业成交额/排名 | 单码 sector kline（复用 market-cycle） | 须确认 sector→kline 代码映射 |
| 行业资金净流入(双榜) | `data_sector` 主力净流入 / 否则聚合成份股 MainNetFlow | 须验证 data_sector 是否含净流入 |
| 成交前 50 个股 | screener 口径 / `data_quote` 批量 | 收盘后快照 |
| 板块集中度 | 前 50 个股 SW1 标签（screener universe.json） | 静态代码池，新股滞后 |
| 量价日 K | 单码 sector kline | 单码口径，无失真 |
| **主力流入流出(拥挤度)** | `data_fund_flow`(MainNetFlow) 聚合成份股 | A/港一致；板块级需聚合 |
| **融资融券(拥挤度)** | `data_fund_margin` 聚合成份股余额变化 | 须验证逐股可得 + 聚合成本 |
| 季度户数(保底) | `data_shareholder` | 仅季报，滞后 |

---

## 9. 已确认设计点
- **A（入口）**：嵌入每日复盘页（主）+ `sector-flow/index.html` 跨日查看器（单独入口）。✅
- **B（拥挤度）**：合成 = 主力净流入 + 融资融券余额变化；季度户数作保底锚。✅

---

## 10. 成功指标
- 连续 5 个交易日自动生成无空窗（changedist 历史限制除外）。
- 任意历史日可通过查看器回看完整模块。
- 双榜/top-5/前50 每日数据完整。

---

## 11. 实现方案纪要（数据源已验证 2026-08-03）
### 11.1 数据源实测结论
- `data_sector` mode=ranking scope=sw1 **一次性返回申万二级行业的**：`zljlr`(主力净流入,万元)、`zljlr_d5/d20`、`zdf`(当日涨跌)、`zdf5/20/60/W52`(阶段涨幅)、`hsl`(换手率)、`cje`(成交额)。→ **双榜 + 阶段涨幅 + 换手率单调用即用，无需聚合成份股**。
- `data_fund_margin` 逐股可得：`FinanceValue`(融资余额)+`FinanceValueDOD`(日变动%)。→ 拥挤度"融资融券"维度，按板块聚合成份股余额变化。
- `data_fund_flow`(MainNetFlow) 仍可作文中交叉验证。

### 11.2 粒度决策（✅ 用户 2026-08-03 拍板：申万二级）
- **双榜/阶段涨幅/换手率**：采用**申万二级行业**（ranking 原生粒度，更细更贴"板块"）。
- **成交前5板块(tracked cards)**：**B 为主口径**——"全市场成交前50个股(screener) → 按申万二级聚类 → 取代表度最高的前5大板块"（忠实于用户框架"成交前50代表板块程度"）；**A 为参考值**——该板块单码成交额全市场排名，并排显示于卡片。
- 双榜全量排序亦按申万二级（31+ 二级行业），与卡片口径统一。

### 11.3 落地形态
- 新建生成器 `.gen_sector_flow.py`（复用 `.gen_market_cycle.py` 的 run/fetch_kline/重试范式）。
- 产出 `stock-review/sector-flow/YYYY-MM-DD.html` + `.json`，并更新 `sector-flow/index.html` 跨日查看器（单独入口）。
- 首页(index.html)与每日复盘页(days/...)加"板块资金流向追踪"链接入口（轻量注入，不重排现有渲染）。
- 接入 15:30 复盘生成层（或紧随其后独立运行），随 16:00 推送上线。

### 11.4 已知缺口（诚实标注）
- 股东户数仍仅季报（保底锚），日频拥挤度=主力净流入+融资融券变化合成，标注"估算非官方"。
- 板块逻辑摘要 v1 留空。

### 11.5 实现路径定稿（CLI 验证后）
- **CLI 不足**：`node … sector ranking sw1` 仅返回"行业资金流入 Top3"（无流出表、无全量排名、无 hsl/cje/阶段涨幅字段）。不能满足双榜+卡片需求。
- **故采用 agent+MCP 自动化**：新建一个交易日 15:35 运行的自动化（agent 类型），prompt 驱动其调用 `data_sector`(ranking)、`data_fund_margin`、`data_kline`(二级板块)、push2 等，写出 `sector-flow/YYYY-MM-DD.html+.json`、刷新 `sector-flow/index.html`、注入首页/每日页入口链接；**不改动现有 15:30 复盘生成器与 market-cycle 生成器**。
- **粒度=申万二级**（✅ 已拍板）。
- **历史回填**：8/3 完整数据；7/27–7/31 尽力回填——仅支持最新快照的接口（ranking 资金净流入、成交前50 等）历史日降级标"待更新"，可回取的（二级板块 kline 量价）尽力填充，诚实标注不臆造。

### 11.6 v1 实现记录（2026-08-03 完成）
- **生成器**：`D:\新建文件夹 (3)\Claw\.gen_sector_flow.py`（Python 主生成器）+ `.gen_sector_flow_universe.py`（构建 `sector-flow/universe_sw2.json`：5511 只 A 股 → 申万二级板块映射，124 个二级行业）。
- **两阶段运行**：`--stage1` 输出 push2 成交前50 + 聚类 top5（供 agent 据此调 MCP）；完整模式读 `sector-flow/_mcp_YYYY-MM-DD.json`（agent 用 MCP 拉的 fundflow 双榜 + 二级板块 kline + 代表股融资融券）渲染最终页面。
- **数据源分工（关键）**：
  - Python 直连：push2 成交前50（主 `push2delay` / 兜底 `push2`，**主端点 2026-08-03 被封，必须用 push2delay**）、universe_sw2 聚类、HTML/JSON 渲染。
  - agent MCP：`data_sector ranking`（双榜主力净流入，**date 参数被忽略仅最新快照**）、`data_kline` 二级板块日K（**CLI kline 不支持二级板块，必须走 MCP**，支持历史日期）、`data_fund_margin`（代表股融资融券，支持历史）。
- **产出**：`sector-flow/2026-08-03.html+.json`（完整：双榜+top5卡片+前50+拥挤度）、`2026-07-27~31.html`（历史回填：top5 量价按日截断，双榜/前50/拥挤度标待更新）、`sector-flow/index.html`（归档，6 日）。
- **入口注入**：首页 `index.html` 新增「💰 板块资金流向追踪」模块 3；每日复盘页 `days/2026-08-03.html` 注入当日模块入口（自动化 prompt 需固化此步骤）。
- **拥挤度合成**：板块级主力净流入方向 + 代表股融资余额日变动（FinanceValueDOD）→ 三档（拥挤升温/中性分歧/拥挤退潮），标注"估算非官方"。
